// En la raiz de la carpeta contenedora, abrir cmd y escribir:
1) npm init -y
2) npm i expres
3) node index  // para correr el servidor
4) ctrl + c para bajar el servidor
5) npm i -g nodemon  // en modo admin en consola. Sirve para hot-reload del proyecto
6) nodemon index // para levantar el proyecto (dentro de la carpeta contenedora del  proyecto)

// Para tomar los puertos dinámicamente

1) npm i dotenv
1) npm install socket.io

// subir al github

1) git init
2) git add .
3) git commit -m "Fin sección 3"
4)
    git remote add origin https://github.com/srobayo/flutter-socket.io-server.git
    git branch -M main
    git push -u origin main
5) git tag -a v1.0.0 -m "Socket Server Básico" (para crear un release)
6) git push --tags

// para crear ids
npm i uuid

// Para subir los cambios a git

git add . //
git commit -m "Cambios sobre bandas"
git push
git remote -v
git tag
git tag -a v1.5.0 -m "Band Socket Server"
git push --tags

// Para publicar en Heroku

1) heroku login
   -- se pone en la consola "del proyecto, en la raiz", se abre el navegador y se pone las credenciales

2) heroku git:remote -a flutter-socket-server-master
   --enter
3) git push heroku master
   -- sube todas las funetes a heroku





